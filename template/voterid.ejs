<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Voter ID</title>
  <style>
    @media print {
      * {
        box-sizing: border-box;
      }
      :root {
        font-size: 16px;
      }
      html,body {
        margin: 0;
        padding: 0;
      }
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
      }
      .identity {
        display: flex;
        margin: 0 auto;
        margin-top: 20px;
      }
      .identity img {
        max-width: 200px;
        width: 100%;
        height: auto;
        margin-right: 20px;
      }   
      .identity p {
        font-size: 0.9rem;
      }
      .title {
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
        margin-bottom: 20px;
      }
      .title img {
        width: 80px;
        margin-right: 20px;
      }
      .title h2 {
        margin: 0
      }
      .title p {
        margin: 5px 0;
      }
      .address p {
        font-size: 0.9rem;
      }
    }
  </style>
</head>
<body>
  <div class="container ">
    <div class="title">
      <div>
        <h2>Voter ID</h2>
        <p>Election Commission</p>
      </div>
    </div>
    <hr />
    <div class="identity">
      <img src="<%= photo %>" alt="<%= name+' '+surname %>" class="photo" />
      <div>
        <p>Name: <%= name+' '+surname %></p>
        <p>Relative: <%= relative %></p>
        <p>Relation: <%= relation %></p>
        <p>Gender: <%= gender %></p>
        <p>Date of Birth: <%= new Date(dob).toISOString().split('T')[0] %></p>
        <p>Disability: <%= disability %></p>
      </div>
    </div>
    <div class="address">
      <p>House Number: <%= address.current.house %></p>
      <p>Street/Area/Locality: <%= address.current.area %></p>
      <p>Town/Village: <%= address.current.block %></p>
      <p>Post: <%= address.current.post %></p>
      <p>District: <%= address.current.district %></p>
      <p>State/UT: <%= address.current.state %></p>
    </div>
    
  </div>
</body>
</html>
